// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               v7.34.1
// source: room.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "fun_kames.schema.room";

export enum Game {
  MonopolyDeal = 0,
  UNRECOGNIZED = -1,
}

export function gameFromJSON(object: any): Game {
  switch (object) {
    case 0:
    case "MonopolyDeal":
      return Game.MonopolyDeal;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Game.UNRECOGNIZED;
  }
}

export function gameToJSON(object: Game): string {
  switch (object) {
    case Game.MonopolyDeal:
      return "MonopolyDeal";
    case Game.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface Player {
  playerId: string;
  displayName: string;
  avatarUrl: string;
  isReady: boolean;
  isHost: boolean;
  joinedAt: number;
}

export interface Room {
  roomId: string;
  displayName: string;
  players: Player[];
  capacity: number;
  occupied: number;
  game: Game;
  settings: Uint8Array;
}

export interface RoomCreated {
  room: Room | undefined;
}

export interface PlayerJoinedRoom {
  roomId: string;
  player: Player | undefined;
}

export interface PlayerLeftRoom {
  roomId: string;
  playedId: string;
  newHostPlayerId?: string | undefined;
}

export interface RoomDeleted {
  roomId: string;
}

export interface Chat {
  payload: string;
}

export interface ChatReceived {
  playerId: string;
  payload: string;
}

export interface PlayerToggledReady {
  playerId: string;
  isReady: boolean;
}

export interface SettingsUpdated {
  capacity: number;
  game: Game;
  settings: Uint8Array;
}

export interface GameStarted {
  gameId: string;
}

export interface ServerMessage {
  roomCreated?: RoomCreated | undefined;
  playerJoinedRoom?: PlayerJoinedRoom | undefined;
  playerLeftRoom?: PlayerLeftRoom | undefined;
  chatReceived?: ChatReceived | undefined;
  playerToggledReady?: PlayerToggledReady | undefined;
  settingsUpdated?: SettingsUpdated | undefined;
  gameStarted?: GameStarted | undefined;
}

export interface ClientMessage {
  chat?: Chat | undefined;
}

function createBasePlayer(): Player {
  return { playerId: "", displayName: "", avatarUrl: "", isReady: false, isHost: false, joinedAt: 0 };
}

export const Player: MessageFns<Player> = {
  encode(message: Player, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.playerId !== "") {
      writer.uint32(10).string(message.playerId);
    }
    if (message.displayName !== "") {
      writer.uint32(18).string(message.displayName);
    }
    if (message.avatarUrl !== "") {
      writer.uint32(26).string(message.avatarUrl);
    }
    if (message.isReady !== false) {
      writer.uint32(32).bool(message.isReady);
    }
    if (message.isHost !== false) {
      writer.uint32(40).bool(message.isHost);
    }
    if (message.joinedAt !== 0) {
      writer.uint32(48).int64(message.joinedAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Player {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayer();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.playerId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.avatarUrl = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.isReady = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.isHost = reader.bool();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.joinedAt = longToNumber(reader.int64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Player {
    return {
      playerId: isSet(object.playerId)
        ? globalThis.String(object.playerId)
        : isSet(object.player_id)
        ? globalThis.String(object.player_id)
        : "",
      displayName: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : isSet(object.display_name)
        ? globalThis.String(object.display_name)
        : "",
      avatarUrl: isSet(object.avatarUrl)
        ? globalThis.String(object.avatarUrl)
        : isSet(object.avatar_url)
        ? globalThis.String(object.avatar_url)
        : "",
      isReady: isSet(object.isReady)
        ? globalThis.Boolean(object.isReady)
        : isSet(object.is_ready)
        ? globalThis.Boolean(object.is_ready)
        : false,
      isHost: isSet(object.isHost)
        ? globalThis.Boolean(object.isHost)
        : isSet(object.is_host)
        ? globalThis.Boolean(object.is_host)
        : false,
      joinedAt: isSet(object.joinedAt)
        ? globalThis.Number(object.joinedAt)
        : isSet(object.joined_at)
        ? globalThis.Number(object.joined_at)
        : 0,
    };
  },

  toJSON(message: Player): unknown {
    const obj: any = {};
    if (message.playerId !== "") {
      obj.playerId = message.playerId;
    }
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    if (message.avatarUrl !== "") {
      obj.avatarUrl = message.avatarUrl;
    }
    if (message.isReady !== false) {
      obj.isReady = message.isReady;
    }
    if (message.isHost !== false) {
      obj.isHost = message.isHost;
    }
    if (message.joinedAt !== 0) {
      obj.joinedAt = Math.round(message.joinedAt);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Player>, I>>(base?: I): Player {
    return Player.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Player>, I>>(object: I): Player {
    const message = createBasePlayer();
    message.playerId = object.playerId ?? "";
    message.displayName = object.displayName ?? "";
    message.avatarUrl = object.avatarUrl ?? "";
    message.isReady = object.isReady ?? false;
    message.isHost = object.isHost ?? false;
    message.joinedAt = object.joinedAt ?? 0;
    return message;
  },
};

function createBaseRoom(): Room {
  return { roomId: "", displayName: "", players: [], capacity: 0, occupied: 0, game: 0, settings: new Uint8Array(0) };
}

export const Room: MessageFns<Room> = {
  encode(message: Room, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.roomId !== "") {
      writer.uint32(10).string(message.roomId);
    }
    if (message.displayName !== "") {
      writer.uint32(18).string(message.displayName);
    }
    for (const v of message.players) {
      Player.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.capacity !== 0) {
      writer.uint32(32).int32(message.capacity);
    }
    if (message.occupied !== 0) {
      writer.uint32(40).int32(message.occupied);
    }
    if (message.game !== 0) {
      writer.uint32(48).int32(message.game);
    }
    if (message.settings.length !== 0) {
      writer.uint32(58).bytes(message.settings);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Room {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRoom();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.roomId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.displayName = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.players.push(Player.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.capacity = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.occupied = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.game = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.settings = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Room {
    return {
      roomId: isSet(object.roomId)
        ? globalThis.String(object.roomId)
        : isSet(object.room_id)
        ? globalThis.String(object.room_id)
        : "",
      displayName: isSet(object.displayName)
        ? globalThis.String(object.displayName)
        : isSet(object.display_name)
        ? globalThis.String(object.display_name)
        : "",
      players: globalThis.Array.isArray(object?.players) ? object.players.map((e: any) => Player.fromJSON(e)) : [],
      capacity: isSet(object.capacity) ? globalThis.Number(object.capacity) : 0,
      occupied: isSet(object.occupied) ? globalThis.Number(object.occupied) : 0,
      game: isSet(object.game) ? gameFromJSON(object.game) : 0,
      settings: isSet(object.settings) ? bytesFromBase64(object.settings) : new Uint8Array(0),
    };
  },

  toJSON(message: Room): unknown {
    const obj: any = {};
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    if (message.players?.length) {
      obj.players = message.players.map((e) => Player.toJSON(e));
    }
    if (message.capacity !== 0) {
      obj.capacity = Math.round(message.capacity);
    }
    if (message.occupied !== 0) {
      obj.occupied = Math.round(message.occupied);
    }
    if (message.game !== 0) {
      obj.game = gameToJSON(message.game);
    }
    if (message.settings.length !== 0) {
      obj.settings = base64FromBytes(message.settings);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Room>, I>>(base?: I): Room {
    return Room.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Room>, I>>(object: I): Room {
    const message = createBaseRoom();
    message.roomId = object.roomId ?? "";
    message.displayName = object.displayName ?? "";
    message.players = object.players?.map((e) => Player.fromPartial(e)) || [];
    message.capacity = object.capacity ?? 0;
    message.occupied = object.occupied ?? 0;
    message.game = object.game ?? 0;
    message.settings = object.settings ?? new Uint8Array(0);
    return message;
  },
};

function createBaseRoomCreated(): RoomCreated {
  return { room: undefined };
}

export const RoomCreated: MessageFns<RoomCreated> = {
  encode(message: RoomCreated, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.room !== undefined) {
      Room.encode(message.room, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RoomCreated {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRoomCreated();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.room = Room.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RoomCreated {
    return { room: isSet(object.room) ? Room.fromJSON(object.room) : undefined };
  },

  toJSON(message: RoomCreated): unknown {
    const obj: any = {};
    if (message.room !== undefined) {
      obj.room = Room.toJSON(message.room);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RoomCreated>, I>>(base?: I): RoomCreated {
    return RoomCreated.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RoomCreated>, I>>(object: I): RoomCreated {
    const message = createBaseRoomCreated();
    message.room = (object.room !== undefined && object.room !== null) ? Room.fromPartial(object.room) : undefined;
    return message;
  },
};

function createBasePlayerJoinedRoom(): PlayerJoinedRoom {
  return { roomId: "", player: undefined };
}

export const PlayerJoinedRoom: MessageFns<PlayerJoinedRoom> = {
  encode(message: PlayerJoinedRoom, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.roomId !== "") {
      writer.uint32(10).string(message.roomId);
    }
    if (message.player !== undefined) {
      Player.encode(message.player, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlayerJoinedRoom {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayerJoinedRoom();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.roomId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.player = Player.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlayerJoinedRoom {
    return {
      roomId: isSet(object.roomId)
        ? globalThis.String(object.roomId)
        : isSet(object.room_id)
        ? globalThis.String(object.room_id)
        : "",
      player: isSet(object.player) ? Player.fromJSON(object.player) : undefined,
    };
  },

  toJSON(message: PlayerJoinedRoom): unknown {
    const obj: any = {};
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    if (message.player !== undefined) {
      obj.player = Player.toJSON(message.player);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlayerJoinedRoom>, I>>(base?: I): PlayerJoinedRoom {
    return PlayerJoinedRoom.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayerJoinedRoom>, I>>(object: I): PlayerJoinedRoom {
    const message = createBasePlayerJoinedRoom();
    message.roomId = object.roomId ?? "";
    message.player = (object.player !== undefined && object.player !== null)
      ? Player.fromPartial(object.player)
      : undefined;
    return message;
  },
};

function createBasePlayerLeftRoom(): PlayerLeftRoom {
  return { roomId: "", playedId: "", newHostPlayerId: undefined };
}

export const PlayerLeftRoom: MessageFns<PlayerLeftRoom> = {
  encode(message: PlayerLeftRoom, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.roomId !== "") {
      writer.uint32(10).string(message.roomId);
    }
    if (message.playedId !== "") {
      writer.uint32(18).string(message.playedId);
    }
    if (message.newHostPlayerId !== undefined) {
      writer.uint32(26).string(message.newHostPlayerId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlayerLeftRoom {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayerLeftRoom();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.roomId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.playedId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.newHostPlayerId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlayerLeftRoom {
    return {
      roomId: isSet(object.roomId)
        ? globalThis.String(object.roomId)
        : isSet(object.room_id)
        ? globalThis.String(object.room_id)
        : "",
      playedId: isSet(object.playedId)
        ? globalThis.String(object.playedId)
        : isSet(object.played_id)
        ? globalThis.String(object.played_id)
        : "",
      newHostPlayerId: isSet(object.newHostPlayerId)
        ? globalThis.String(object.newHostPlayerId)
        : isSet(object.new_host_player_id)
        ? globalThis.String(object.new_host_player_id)
        : undefined,
    };
  },

  toJSON(message: PlayerLeftRoom): unknown {
    const obj: any = {};
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    if (message.playedId !== "") {
      obj.playedId = message.playedId;
    }
    if (message.newHostPlayerId !== undefined) {
      obj.newHostPlayerId = message.newHostPlayerId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlayerLeftRoom>, I>>(base?: I): PlayerLeftRoom {
    return PlayerLeftRoom.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayerLeftRoom>, I>>(object: I): PlayerLeftRoom {
    const message = createBasePlayerLeftRoom();
    message.roomId = object.roomId ?? "";
    message.playedId = object.playedId ?? "";
    message.newHostPlayerId = object.newHostPlayerId ?? undefined;
    return message;
  },
};

function createBaseRoomDeleted(): RoomDeleted {
  return { roomId: "" };
}

export const RoomDeleted: MessageFns<RoomDeleted> = {
  encode(message: RoomDeleted, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.roomId !== "") {
      writer.uint32(10).string(message.roomId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RoomDeleted {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRoomDeleted();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.roomId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RoomDeleted {
    return {
      roomId: isSet(object.roomId)
        ? globalThis.String(object.roomId)
        : isSet(object.room_id)
        ? globalThis.String(object.room_id)
        : "",
    };
  },

  toJSON(message: RoomDeleted): unknown {
    const obj: any = {};
    if (message.roomId !== "") {
      obj.roomId = message.roomId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RoomDeleted>, I>>(base?: I): RoomDeleted {
    return RoomDeleted.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RoomDeleted>, I>>(object: I): RoomDeleted {
    const message = createBaseRoomDeleted();
    message.roomId = object.roomId ?? "";
    return message;
  },
};

function createBaseChat(): Chat {
  return { payload: "" };
}

export const Chat: MessageFns<Chat> = {
  encode(message: Chat, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.payload !== "") {
      writer.uint32(10).string(message.payload);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Chat {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChat();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.payload = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Chat {
    return { payload: isSet(object.payload) ? globalThis.String(object.payload) : "" };
  },

  toJSON(message: Chat): unknown {
    const obj: any = {};
    if (message.payload !== "") {
      obj.payload = message.payload;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Chat>, I>>(base?: I): Chat {
    return Chat.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Chat>, I>>(object: I): Chat {
    const message = createBaseChat();
    message.payload = object.payload ?? "";
    return message;
  },
};

function createBaseChatReceived(): ChatReceived {
  return { playerId: "", payload: "" };
}

export const ChatReceived: MessageFns<ChatReceived> = {
  encode(message: ChatReceived, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.playerId !== "") {
      writer.uint32(10).string(message.playerId);
    }
    if (message.payload !== "") {
      writer.uint32(18).string(message.payload);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ChatReceived {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChatReceived();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.playerId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.payload = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ChatReceived {
    return {
      playerId: isSet(object.playerId)
        ? globalThis.String(object.playerId)
        : isSet(object.player_id)
        ? globalThis.String(object.player_id)
        : "",
      payload: isSet(object.payload) ? globalThis.String(object.payload) : "",
    };
  },

  toJSON(message: ChatReceived): unknown {
    const obj: any = {};
    if (message.playerId !== "") {
      obj.playerId = message.playerId;
    }
    if (message.payload !== "") {
      obj.payload = message.payload;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ChatReceived>, I>>(base?: I): ChatReceived {
    return ChatReceived.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ChatReceived>, I>>(object: I): ChatReceived {
    const message = createBaseChatReceived();
    message.playerId = object.playerId ?? "";
    message.payload = object.payload ?? "";
    return message;
  },
};

function createBasePlayerToggledReady(): PlayerToggledReady {
  return { playerId: "", isReady: false };
}

export const PlayerToggledReady: MessageFns<PlayerToggledReady> = {
  encode(message: PlayerToggledReady, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.playerId !== "") {
      writer.uint32(10).string(message.playerId);
    }
    if (message.isReady !== false) {
      writer.uint32(16).bool(message.isReady);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PlayerToggledReady {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePlayerToggledReady();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.playerId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.isReady = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PlayerToggledReady {
    return {
      playerId: isSet(object.playerId)
        ? globalThis.String(object.playerId)
        : isSet(object.player_id)
        ? globalThis.String(object.player_id)
        : "",
      isReady: isSet(object.isReady)
        ? globalThis.Boolean(object.isReady)
        : isSet(object.is_ready)
        ? globalThis.Boolean(object.is_ready)
        : false,
    };
  },

  toJSON(message: PlayerToggledReady): unknown {
    const obj: any = {};
    if (message.playerId !== "") {
      obj.playerId = message.playerId;
    }
    if (message.isReady !== false) {
      obj.isReady = message.isReady;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PlayerToggledReady>, I>>(base?: I): PlayerToggledReady {
    return PlayerToggledReady.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PlayerToggledReady>, I>>(object: I): PlayerToggledReady {
    const message = createBasePlayerToggledReady();
    message.playerId = object.playerId ?? "";
    message.isReady = object.isReady ?? false;
    return message;
  },
};

function createBaseSettingsUpdated(): SettingsUpdated {
  return { capacity: 0, game: 0, settings: new Uint8Array(0) };
}

export const SettingsUpdated: MessageFns<SettingsUpdated> = {
  encode(message: SettingsUpdated, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.capacity !== 0) {
      writer.uint32(8).int32(message.capacity);
    }
    if (message.game !== 0) {
      writer.uint32(16).int32(message.game);
    }
    if (message.settings.length !== 0) {
      writer.uint32(26).bytes(message.settings);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SettingsUpdated {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSettingsUpdated();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.capacity = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.game = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.settings = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SettingsUpdated {
    return {
      capacity: isSet(object.capacity) ? globalThis.Number(object.capacity) : 0,
      game: isSet(object.game) ? gameFromJSON(object.game) : 0,
      settings: isSet(object.settings) ? bytesFromBase64(object.settings) : new Uint8Array(0),
    };
  },

  toJSON(message: SettingsUpdated): unknown {
    const obj: any = {};
    if (message.capacity !== 0) {
      obj.capacity = Math.round(message.capacity);
    }
    if (message.game !== 0) {
      obj.game = gameToJSON(message.game);
    }
    if (message.settings.length !== 0) {
      obj.settings = base64FromBytes(message.settings);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SettingsUpdated>, I>>(base?: I): SettingsUpdated {
    return SettingsUpdated.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SettingsUpdated>, I>>(object: I): SettingsUpdated {
    const message = createBaseSettingsUpdated();
    message.capacity = object.capacity ?? 0;
    message.game = object.game ?? 0;
    message.settings = object.settings ?? new Uint8Array(0);
    return message;
  },
};

function createBaseGameStarted(): GameStarted {
  return { gameId: "" };
}

export const GameStarted: MessageFns<GameStarted> = {
  encode(message: GameStarted, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.gameId !== "") {
      writer.uint32(10).string(message.gameId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GameStarted {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGameStarted();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.gameId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GameStarted {
    return {
      gameId: isSet(object.gameId)
        ? globalThis.String(object.gameId)
        : isSet(object.game_id)
        ? globalThis.String(object.game_id)
        : "",
    };
  },

  toJSON(message: GameStarted): unknown {
    const obj: any = {};
    if (message.gameId !== "") {
      obj.gameId = message.gameId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GameStarted>, I>>(base?: I): GameStarted {
    return GameStarted.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GameStarted>, I>>(object: I): GameStarted {
    const message = createBaseGameStarted();
    message.gameId = object.gameId ?? "";
    return message;
  },
};

function createBaseServerMessage(): ServerMessage {
  return {
    roomCreated: undefined,
    playerJoinedRoom: undefined,
    playerLeftRoom: undefined,
    chatReceived: undefined,
    playerToggledReady: undefined,
    settingsUpdated: undefined,
    gameStarted: undefined,
  };
}

export const ServerMessage: MessageFns<ServerMessage> = {
  encode(message: ServerMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.roomCreated !== undefined) {
      RoomCreated.encode(message.roomCreated, writer.uint32(10).fork()).join();
    }
    if (message.playerJoinedRoom !== undefined) {
      PlayerJoinedRoom.encode(message.playerJoinedRoom, writer.uint32(18).fork()).join();
    }
    if (message.playerLeftRoom !== undefined) {
      PlayerLeftRoom.encode(message.playerLeftRoom, writer.uint32(26).fork()).join();
    }
    if (message.chatReceived !== undefined) {
      ChatReceived.encode(message.chatReceived, writer.uint32(34).fork()).join();
    }
    if (message.playerToggledReady !== undefined) {
      PlayerToggledReady.encode(message.playerToggledReady, writer.uint32(42).fork()).join();
    }
    if (message.settingsUpdated !== undefined) {
      SettingsUpdated.encode(message.settingsUpdated, writer.uint32(50).fork()).join();
    }
    if (message.gameStarted !== undefined) {
      GameStarted.encode(message.gameStarted, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseServerMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.roomCreated = RoomCreated.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.playerJoinedRoom = PlayerJoinedRoom.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.playerLeftRoom = PlayerLeftRoom.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.chatReceived = ChatReceived.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.playerToggledReady = PlayerToggledReady.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.settingsUpdated = SettingsUpdated.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.gameStarted = GameStarted.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ServerMessage {
    return {
      roomCreated: isSet(object.roomCreated)
        ? RoomCreated.fromJSON(object.roomCreated)
        : isSet(object.room_created)
        ? RoomCreated.fromJSON(object.room_created)
        : undefined,
      playerJoinedRoom: isSet(object.playerJoinedRoom)
        ? PlayerJoinedRoom.fromJSON(object.playerJoinedRoom)
        : isSet(object.player_joined_room)
        ? PlayerJoinedRoom.fromJSON(object.player_joined_room)
        : undefined,
      playerLeftRoom: isSet(object.playerLeftRoom)
        ? PlayerLeftRoom.fromJSON(object.playerLeftRoom)
        : isSet(object.player_left_room)
        ? PlayerLeftRoom.fromJSON(object.player_left_room)
        : undefined,
      chatReceived: isSet(object.chatReceived)
        ? ChatReceived.fromJSON(object.chatReceived)
        : isSet(object.chat_received)
        ? ChatReceived.fromJSON(object.chat_received)
        : undefined,
      playerToggledReady: isSet(object.playerToggledReady)
        ? PlayerToggledReady.fromJSON(object.playerToggledReady)
        : isSet(object.player_toggled_ready)
        ? PlayerToggledReady.fromJSON(object.player_toggled_ready)
        : undefined,
      settingsUpdated: isSet(object.settingsUpdated)
        ? SettingsUpdated.fromJSON(object.settingsUpdated)
        : isSet(object.settings_updated)
        ? SettingsUpdated.fromJSON(object.settings_updated)
        : undefined,
      gameStarted: isSet(object.gameStarted)
        ? GameStarted.fromJSON(object.gameStarted)
        : isSet(object.game_started)
        ? GameStarted.fromJSON(object.game_started)
        : undefined,
    };
  },

  toJSON(message: ServerMessage): unknown {
    const obj: any = {};
    if (message.roomCreated !== undefined) {
      obj.roomCreated = RoomCreated.toJSON(message.roomCreated);
    }
    if (message.playerJoinedRoom !== undefined) {
      obj.playerJoinedRoom = PlayerJoinedRoom.toJSON(message.playerJoinedRoom);
    }
    if (message.playerLeftRoom !== undefined) {
      obj.playerLeftRoom = PlayerLeftRoom.toJSON(message.playerLeftRoom);
    }
    if (message.chatReceived !== undefined) {
      obj.chatReceived = ChatReceived.toJSON(message.chatReceived);
    }
    if (message.playerToggledReady !== undefined) {
      obj.playerToggledReady = PlayerToggledReady.toJSON(message.playerToggledReady);
    }
    if (message.settingsUpdated !== undefined) {
      obj.settingsUpdated = SettingsUpdated.toJSON(message.settingsUpdated);
    }
    if (message.gameStarted !== undefined) {
      obj.gameStarted = GameStarted.toJSON(message.gameStarted);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServerMessage>, I>>(base?: I): ServerMessage {
    return ServerMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServerMessage>, I>>(object: I): ServerMessage {
    const message = createBaseServerMessage();
    message.roomCreated = (object.roomCreated !== undefined && object.roomCreated !== null)
      ? RoomCreated.fromPartial(object.roomCreated)
      : undefined;
    message.playerJoinedRoom = (object.playerJoinedRoom !== undefined && object.playerJoinedRoom !== null)
      ? PlayerJoinedRoom.fromPartial(object.playerJoinedRoom)
      : undefined;
    message.playerLeftRoom = (object.playerLeftRoom !== undefined && object.playerLeftRoom !== null)
      ? PlayerLeftRoom.fromPartial(object.playerLeftRoom)
      : undefined;
    message.chatReceived = (object.chatReceived !== undefined && object.chatReceived !== null)
      ? ChatReceived.fromPartial(object.chatReceived)
      : undefined;
    message.playerToggledReady = (object.playerToggledReady !== undefined && object.playerToggledReady !== null)
      ? PlayerToggledReady.fromPartial(object.playerToggledReady)
      : undefined;
    message.settingsUpdated = (object.settingsUpdated !== undefined && object.settingsUpdated !== null)
      ? SettingsUpdated.fromPartial(object.settingsUpdated)
      : undefined;
    message.gameStarted = (object.gameStarted !== undefined && object.gameStarted !== null)
      ? GameStarted.fromPartial(object.gameStarted)
      : undefined;
    return message;
  },
};

function createBaseClientMessage(): ClientMessage {
  return { chat: undefined };
}

export const ClientMessage: MessageFns<ClientMessage> = {
  encode(message: ClientMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.chat !== undefined) {
      Chat.encode(message.chat, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClientMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClientMessage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.chat = Chat.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ClientMessage {
    return { chat: isSet(object.chat) ? Chat.fromJSON(object.chat) : undefined };
  },

  toJSON(message: ClientMessage): unknown {
    const obj: any = {};
    if (message.chat !== undefined) {
      obj.chat = Chat.toJSON(message.chat);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ClientMessage>, I>>(base?: I): ClientMessage {
    return ClientMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ClientMessage>, I>>(object: I): ClientMessage {
    const message = createBaseClientMessage();
    message.chat = (object.chat !== undefined && object.chat !== null) ? Chat.fromPartial(object.chat) : undefined;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
