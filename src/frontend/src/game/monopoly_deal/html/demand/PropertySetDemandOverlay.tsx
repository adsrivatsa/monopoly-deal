import { type Demand, type Player } from "../../../../generated/monopoly_deal";

type PropertySetDemandOverlayProps = {
  demand: Demand;
  players: Player[];
};

const PropertySetDemandOverlay = (_props: PropertySetDemandOverlayProps) => {
  return null;
};

export default PropertySetDemandOverlay;
